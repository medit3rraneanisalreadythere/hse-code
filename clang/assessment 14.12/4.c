#include <stdio.h> // подключение необходимых заголовочных файлов

struct tree {
    int data;
    struct tree *left;
    struct tree *right;
};

long long banana(struct tree *t) {
    t->left;
}
